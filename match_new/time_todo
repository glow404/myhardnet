1.关闭cuDNN，每次提取一张指纹图像的特征时，输入的结构是[B,1,32，32]，B为关键点个数，patch数量。
cudnn的做法是第一次遇到某个输入形状-》把多个卷积算法都实际运行一遍-》测量谁最快-》缓存该形状对应的最快算法-》以后相同形状直接使用缓存结果
但是关键点数量不一定相同，出现新的关键点数量时，推理时间显著增加。
解决办法：
1.1关闭cuDNN，卷积算法可选择为Winograd或者Implicit GEMM。
1.2关键点补齐为同一个数值

2，模型加载和预热不统计总时间当中

3.旋转，裁patch很占时间。
旧方案
原图
  ↓ warpAffine，第一次线性插值
完整旋转图
  ↓ getRectSubPix，第二次插值
patch
  ↓ resize
最终 patch

新方案
原图
  ↓ warpAffine，一次插值
最终 patch

