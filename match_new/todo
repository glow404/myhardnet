鉴权分类：
可以考虑的新的匹配参数指标
1.能够包住所有内点的最小凸多边形的大小 
分散较好                 集中在狭窄条带

+----------------+       +----------------+
|      ●         |       |          ●●    |
|                |       |          ●     |
|  ●         ●   |       |          ●●    |
|                |       |           ●    |
|       ●        |       |          ●     |
+----------------+       +----------------+
凸包面积较大              凸包面积很小
; 2.内点网格覆盖率
3.patch对之间的重叠率overlap
4.方向一致性
5.模板学习

6.脚本优化

7.show出内点少的样本


8.模板压缩
当前模板主要包含：
keypoints_xy
keypoints_angle
keypoints_size
keypoints_response
hardnet_descriptors[N,128]，当前为 float32
HardNet 描述子就占：
\[
300\times128\times4=153600\text{ bytes}\approx150\text{ KB}
\]

8.1
目前模板本身就会通过np.savez_compressed(...)进行压缩。


np.load(...)



代码层面修改：
1.deployment.py作用，现在不需要离线和在线匹配参数一致。（在线匹配主要是为了测量优化时间）
2.hardnet与sift对比的部分全部删除。
3.时间优化：
3.1推理加速
3.2图像裁剪后不旋转或者不做截取，而是直接输入对应区域数据？
3.3统计各个部分的耗时
4.统计解锁耗时，只需要统计成功解锁的时间，而不是每个指纹的平均全流程时间。



5.
5.1模型结构改变，模型大小变小，#卷积改为十字卷积
5.2损失函数改变，改为topk最难样本
5.3输出的描述子二值化，（1.直接训练的结果就是0101的形式，2.根据浩哥的方案，训练时接近-1，1，在后续对得到的128维浮点的描述子处理为二值化的）
5.5  FAR,FRR指标要达标，主要靠筛选数据集，鉴权分类，修改模型来达到
5.6  裁剪大小改为16*16，模型结果，数据集构建全部推翻，重新训练一遍。

5.7关于裁剪patch块的旋转问题，如果数据集构建的时候没有做旋转对齐，这样得到的训练结果可不可以在实际的应用中，最终推理时不需要做旋转对齐。
5.8 将“将要提交"，实际实际工程的代码和测试指标的代码区分开来，保持相互独立。