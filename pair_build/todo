几何扩展正样本可能还需要调整
当前max_positive_per_image_pair: 16 每个图像对最多16个正样本
2. 硬条件过滤
候选必须满足：

投影距离不超过2像素
方向误差不超过30°
尺度比例在 [0.70, 1.40]
两侧关键点距离边界至少16像素
两侧局部纹理标准差不低于5
不能和已有SIFT内点重复

3.稳定性评分
通过硬条件后计算 stability_score，包括：
SIFT描述子距离
重投影误差
方向误差
尺度一致性
关键点响应强度
局部纹理质量
是否为直接SIFT内点

空间去重后，所有来源混合排序，取前16

4.不同手指的物理点的描述子距离较大，但同手指的物理点的次近邻描述子距离较小。